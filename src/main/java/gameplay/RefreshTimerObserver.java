package gameplay;

/**
 * An interface responsible for the refresh of the GUI
 * @author Daniel Pavenko
 */
public interface RefreshTimerObserver {
  /**
   * Outlines a response to being updated by a RefreshTimer subject
   */
  void updateRefresh();
}
